# Mobile API URL overrides (optional)
# If unset, app uses the production Render backend by default.
# For Android emulator local backend use: http://10.0.2.2:3000
# For iOS simulator local backend use: http://localhost:3000
REKONO_API_URL=
EXPO_PUBLIC_REKONO_API_URL=
